; Comments
(comment) @comment

; HEEx attribute values
[
  (attribute_value)
  (quoted_attribute_value)
] @string

; Function identifiers
(function) @function

; Slot identifiers
(slot_name) @slot

; Elixir expressions
[
  (expression)
  (directive)
  (component_name)
] @elixir
